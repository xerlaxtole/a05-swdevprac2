# Venue Explorer — Assignment A05

React events, interaction, and MUI form.

- `/` — banner + venue cards (`Card` → `InteractiveCard`, hover changes shadow/background)
- `/booking` — MUI booking form (`DateReserve`: DatePicker, Name-Lastname, Contact-Number, venue Select)

## Vercel

TODO: paste deployed URL here

## Run

```bash
npm install
npm run dev
npm run test
```
